{
  "name": "driver-awareness-demo",
  "version": "0.2.0",
  "author": "Jake G",
  "description": "Demo for driver awareness",
  "repository" : "dev",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --out=dist --app-version=$npm_package_version --prune --asar --overwrite --all"
  },
  "devDependencies": {
    "electron-packager": "^7.0.1",
    "electron-prebuilt": "^0.37.8",
    "electron-rebuild": "^1.1.3"
  },
  "dependencies": {
    "zmq": "^2.15.0"
  }
}
